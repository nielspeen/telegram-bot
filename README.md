Simple Telegram bot to moderate chats using OpenAI.

* Copy .env.example to .env and set the variables.
* Run `php artisan key:generate`
* Run `php artisan migrate`
* Run `php artisan telegram:set-webhook`